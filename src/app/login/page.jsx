import React from 'react';

export const metadata = {
  title: "SkillSphere | Login",
};

const LoginPage = () => {
    return (
        <div>
            <h1 className='font-black'> Login</h1>
        </div>
    );
};

export default LoginPage;